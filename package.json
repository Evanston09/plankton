{
  "author": "Evan Kim",
  "name": "plankton-workspace",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "format": "prettier . --write",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm build && vitest run",
    "check": "pnpm test && pnpm typecheck",
    "plankton": "node packages/cli/dist/cli.js"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.2",
    "@types/node": "^22.0.0",
    "prettier": "3.9.6",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
